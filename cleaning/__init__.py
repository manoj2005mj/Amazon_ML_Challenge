"""Data cleaning stage (raw TSVs -> cleaned TSVs + sidecars) feeding the blocking gate.

See ``cleaning.clean`` (engine + CLI), ``cleaning.rules`` (static tables),
``cleaning.learn_dictionary`` (ground-truth-aligned transliteration and
abbreviation dictionaries) and ``cleaning.segment`` (website-name segmentation).
"""
